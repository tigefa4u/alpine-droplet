name: Build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      - uses: actions/checkout@main
        with:
          submodules: true

      - name: Install dependencies
        run: sudo apt install qemu-utils bzip2

      - name: Build
        run: |
          sudo -E ./build-image.sh
          ls -hal
          sudo chown -fR $USER:$USER $PWD
          ls -hal
          wget --quiet https://dl.min.io/client/mc/release/linux-amd64/mc -O /usr/local/bin/mc && chmod +x /usr/local/bin/mc
          mc alias set sgp1 $DOS_SGP1 $DOS_KEY $DOS_SECRET --api S3v4
          mc cp --recursive '$PWD' sgp1/tigefa/alpine/

      - uses: actions/upload-artifact@main
        with:
          name: alpine-virt-image.qcow2.bz2
          path: "*.bz2"

      - name: Read version
        id: getversion
        run: echo "::set-output name=version::$(cat version)"

      - uses: marvinpinto/action-automatic-releases@latest
        if: github.ref == 'refs/heads/main'
        with:
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          files: "*.bz2"
          automatic_release_tag: ${{ steps.getversion.outputs.version }}
          prerelease: false
